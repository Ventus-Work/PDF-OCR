# Phase 9 Step 2 결과 보고서

- **작업일**: 2026-04-20
- **브랜치**: `phase9-step2`
- **태그**: `phase9-step2-complete` → `3d83c2b`
- **기준**: `phase9-step1-complete` → `165c4cc` (137 passed)
- **목적**: `pipelines/` 패키지 신설 — BOM·문서 파이프라인 분리

---

## 1. 산출물 목록 (5개 파일)

| # | 파일 | 역할 | 원본 위치 |
|---|------|------|-----------|
| 2.1 | `pipelines/__init__.py` | 공개 API 노출 (`BasePipeline`, `PipelineContext`, `create_pipeline`) | 신규 |
| 2.2 | `pipelines/base.py` | `PipelineContext` 데이터클래스 + `BasePipeline` ABC | 신규 (spec §4.2) |
| 2.3 | `pipelines/bom_pipeline.py` | OCR → BOM 구조화 → JSON/Excel 출력 | main.py L348-418 이식 |
| 2.4 | `pipelines/document_pipeline.py` | pumsem/estimate/범용 통합 | main.py L420-595 이식 |
| 2.5 | `pipelines/factory.py` | 프리셋 → 파이프라인 매핑 | 신규 |

---

## 2. 설계 결정 사항

### 2.1 단일 책임 원칙 준수 (`BomPipeline`)

- `BomPipeline.run()` 은 **단일 PDF 1건만** 처리
- 배치 집계 (`export_aggregated_excel`) 는 main.py(호출자)가 담당
- `BomPipeline` 완료 후 호출자가 후처리로 `export_aggregated_excel()` 호출

### 2.2 엔진 화이트리스트 (이중 보증)

| 파이프라인 | 허용 엔진 | 근거 |
|-----------|-----------|------|
| `DocumentPipeline` | `gemini`, `local` | main.py L446-448 원본 동작 보존 |
| `BomPipeline` | `zai`, `mistral`, `tesseract`, `local` | OCR 지원 엔진만 허용 |

### 2.3 프리셋 리소스 로딩 내부화

`DocumentPipeline._load_preset()` 이 preset 별 리소스를 `dict` 로 반환:

```python
{
  "division_names": [...],   # pumsem
  "parse_patterns": {...},   # pumsem
  "type_keywords": {...},    # pumsem / estimate
  "excel_config": {...},     # estimate
}
```

---

## 3. 테스트 결과

### 3.1 신규 테스트 파일 (16개 케이스)

| 파일 | 케이스 수 | 결과 |
|------|-----------|------|
| `tests/unit/pipelines/test_base.py` | 5 | ✅ all passed |
| `tests/unit/pipelines/test_bom_pipeline.py` | 3 | ✅ all passed |
| `tests/unit/pipelines/test_document_pipeline.py` | 4 | ✅ all passed |
| `tests/unit/pipelines/test_pipeline_factory.py` | 4 | ✅ all passed |

### 3.2 전체 pytest 결과

```
python -m pytest tests/unit/ -p no:capture --tb=short -q
→ 153 passed, 1 skipped in 3.33s
```

| 구분 | Step 1 완료 후 | Step 2 완료 후 |
|------|----------------|----------------|
| passed | 137 | **153** (+16) |
| skipped | 1 | 1 |
| failed | 0 | **0** |

---

## 4. 특이사항 및 수리 내역

### 4.1 테스트 파일명 충돌 (`test_factory`)

- **현상**: `tests/unit/pipelines/test_factory.py` ↔ `tests/unit/engines/test_factory.py` 모듈명 충돌
  - pytest 수집 오류: `import file mismatch`
- **시도**: `__init__.py` 추가 → 기존 패키지(`parsers`, `engines` 등)와 네임스페이스 충돌 발생
- **최종 해결**: `test_factory.py` → **`test_pipeline_factory.py`** 로 파일명 변경 (기존 패턴 유지)

### 4.2 `_get_output_base` 테스트 패치 불가

- **현상**: `base.py` 내 `datetime` 이 함수 내부 lazy import → `patch("pipelines.base.datetime")` 불가
- **해결**: 절대값 대신 **형식 검증** (날짜 8자리, 줄기명 + 접미사 포함)으로 테스트 수정

---

## 5. 커밋 이력 (Step 2)

```
3d83c2b  refactor(step2): add pipelines/ package + tests (153 passed)  ← phase9-step2-complete
165c4cc  refactor(step1): extract cli/args.py + --no-cache flag + tests (137 passed)  ← phase9-step1-complete
```

---

## 6. Step 2 체크포인트 완료 여부

```
✅ 5개 파일 생성 완료
✅ 신규 16개 파이프라인 테스트 통과 (spec 10개 초과)
✅ 기존 137개 테스트 100% 통과 (회귀 0건)
✅ _validate_engine — zai/mistral/tesseract 거부 (DocumentPipeline)
✅ BomPipeline — gemini 거부 (OCR 미지원)
✅ git tag phase9-step2-complete (3d83c2b)
✅ main.py 아직 수정 안 함 (792줄 유지)
```

---

## 7. Phase 9 Step 3 착수 조건

```
✅ 위 체크포인트 전부 충족

다음 단계: parsers/types.py TypedDict (spec §5)
  - TableCell, ParsedTable, ParsedSection, TocEntry
  - 기간: 1~2일 / 위험: 낮음

착수 명령:
  git checkout -b phase9-step3
```
