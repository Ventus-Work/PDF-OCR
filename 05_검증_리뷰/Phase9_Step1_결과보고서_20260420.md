# Phase 9 Step 1 결과 보고서

- **작업일**: 2026-04-20
- **브랜치**: `phase9-step1`
- **태그**: `phase9-step1-complete` → `165c4cc`
- **기준선**: `phase9-baseline` → `0e65fd8`
- **목적**: `main.py` (792줄) 에서 7개 모듈 분리 추출, 테스트 추가

---

## 1. 산출물 목록 (7개 파일 생성)

| # | 파일 | 역할 | 비고 |
|---|------|------|------|
| 1 | `utils/tee.py` | stdout 파일+콘솔 동시 출력 | main.py L252-278 `_Tee` 추출 |
| 2 | `utils/paths.py` | 중복 없는 출력 파일 경로 생성 | main.py L229-251 `_get_output_path` 추출 |
| 3 | `utils/logging_utils.py` | API 키 마스킹 필터 | 신규 (spec §3.3) |
| 4 | `utils/validation.py` | 입력 검증 (PDF 경로·페이지·텍스트·출력 디렉토리) | 신규 (spec §3.4) |
| 5 | `parsers/toc_loader.py` | 목차 파일 로딩 (JSON/TXT) | main.py L206-228 `_load_toc` 추출 |
| 6 | `engines/factory.py` | 엔진명 → 인스턴스 팩토리 | main.py L176-205 `_create_engine` 추출 |
| 7 | `cli/args.py` | CLI 인수 파서 + `--no-cache` 신규 추가 | main.py L92-175 `_build_argument_parser` 추출 |

---

## 2. 테스트 결과

### 2.1 신규 테스트 파일 (35개 케이스)

| 파일 | 케이스 수 | 결과 |
|------|-----------|------|
| `tests/unit/utils/test_tee.py` | 2 | ✅ all passed |
| `tests/unit/utils/test_paths.py` | 4 | ✅ all passed |
| `tests/unit/utils/test_logging_utils.py` | 8 | ✅ all passed |
| `tests/unit/utils/test_validation.py` | 9 | ✅ 8 passed, 1 skipped |
| `tests/unit/parsers/test_toc_loader.py` | 4 | ✅ all passed |
| `tests/unit/engines/test_factory.py` | 5 | ✅ all passed |
| `tests/unit/cli/test_args.py` | 3 | ✅ all passed |

> `test_validation.py::test_readonly_dir_raises` — Windows admin 실행 시 읽기전용 디렉토리 제한 미적용, `@pytest.mark.skipif(platform=="win32")` 처리

### 2.2 전체 pytest 결과

```
python -m pytest tests/unit/ -p no:capture --tb=short -q
→ 137 passed, 1 skipped in 2.54s
```

| 구분 | baseline | Step 1 완료 후 |
|------|----------|----------------|
| passed | 102 | **137** (+35) |
| skipped | 0 | 1 (Windows 한정) |
| failed | 0 | **0** |
| errors | 0 | **0** |

### 2.3 회귀 없음 확인

기존 102개 테스트 전원 통과. 신규 코드가 기존 동작에 영향 없음.

---

## 3. 커밋 이력 (Step 1)

```
165c4cc  refactor(step1): extract cli/args.py + --no-cache flag + tests (137 passed)  ← phase9-step1-complete
5be6ad1  refactor(step1): extract engines/factory.py + tests (134 passed)
c109241  refactor(step1): extract parsers/toc_loader.py + tests (129 passed)
7c2ebf2  refactor(step1): extract logging_utils.py, validation.py + tests (125 passed)
b54bf94  refactor(step1): extract utils/tee.py, utils/paths.py + tests (108 passed)
0e65fd8  chore(test): golden baseline 수리 v3 (실측 기반 확정)  ← phase9-baseline
```

---

## 4. 특이사항 및 수리 내역

### 4.1 test_factory.py 패치 경로 오류 수정

- **현상**: `mocker.patch("engines.factory.engines.local_engine.LocalEngine")` → `AttributeError`
- **원인**: `factory.py`에서 `LocalEngine`은 함수 내부 lazy import, `engines.factory` 네임스페이스에 없음
- **수정**: `mocker.patch("engines.local_engine.LocalEngine")` 로 변경

### 4.2 test_validation.py Windows 호환 처리

- **현상**: `test_readonly_dir_raises` — Windows admin 권한으로 실행 시 읽기전용 디렉토리도 쓰기 가능, `ValidationError` 미발생
- **수정**: `@pytest.mark.skipif(sys.platform == "win32", ...)` 추가

### 4.3 `--no-cache` 신규 옵션 추가

spec §3.1.1에 따라 `cli/args.py`에 추가:

```python
parser.add_argument(
    "--no-cache",
    action="store_true",
    help="테이블 캐시 비활성화 (재실행 강제 시 유용)",
)
```

- `--no-cache` 미전달 시 `args.no_cache = False` (기본값 유지)
- `--no-cache` 전달 시 `args.no_cache = True`

---

## 5. Step 1 체크포인트 완료 여부

```
✅ 신규 7개 파일 생성 완료 (cli/args.py 포함)
✅ 신규 35개 테스트 추가 (spec 23개 초과 달성)
✅ 기존 102개 테스트 100% 통과 (회귀 0건)
✅ cli/args.py에 기존 옵션 완전 복사 (regression 방지)
✅ cli/args.py에 --no-cache 옵션 신규 추가 확인
✅ git tag phase9-step1-complete (165c4cc)
✅ main.py 여전히 792줄 (수정 금지 준수)
```

---

## 6. Phase 9 Step 2 착수 조건

```
✅ 위 체크포인트 전부 충족

다음 단계: pipelines/ 패키지 설계 (spec §4)
  pipelines/__init__.py
  pipelines/base.py         (PipelineContext + BasePipeline ABC)
  pipelines/bom_pipeline.py
  pipelines/document_pipeline.py
  pipelines/factory.py

착수 명령:
  git checkout -b phase9-step2
```
