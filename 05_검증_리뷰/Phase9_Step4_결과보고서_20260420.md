# Phase 9 Step 4 결과보고서 — main.py 슬림화

- **날짜**: 2026-04-20
- **브랜치**: `phase9-step4`
- **태그**: `phase9-step4-complete`
- **커밋**: `013a8c4` (A) → `aba5a29` (B+C)

---

## 목표

`main.py` 792줄 → ≤350줄로 슬림화.  
비즈니스 로직을 모두 `pipelines/`, `cli/`, `utils/` 패키지로 이전 완료.

---

## 작업 내역

### Commit A (`013a8c4`)
- `_process_single()` 내부 분기(BOM/문서) 로직을 `PipelineContext` + `create_pipeline().run()` 으로 교체
- 잔여 헬퍼 함수들 유지 (중간 안전 지점)
- 결과: 494줄, 161 passed, Golden MATCH

### Commit B+C (`aba5a29`)
- 기존 헬퍼 함수 5개 제거 (`_build_argument_parser`, `_create_engine`, `_load_toc`, `_get_output_path`, `_Tee`)
- 신규 얇은 헬퍼 3개 추가 (`_collect_inputs`, `_init_cache`, `_run_bom_aggregation`)
- `main()` 은 `_process_single` → `create_pipeline(ctx).run()` 위임만 수행
- 결과: **238줄** (목표 ≤350 달성), 161 passed, Golden MATCH

---

## 검증 결과

| 항목 | 결과 |
|---|---|
| `main.py` 줄 수 | **238줄** (목표 ≤350) |
| `pytest tests/unit/` | **161 passed, 1 skipped** |
| Golden E2E (`generic.md → JSON`) | **MATCH** |
| GitHub push | `phase9-step4` 브랜치 + 태그 푸시 완료 |

---

## 제거된 코드 (main.py에서 완전 삭제)

| 헬퍼 | 이전된 위치 |
|---|---|
| `_build_argument_parser()` | `cli/args.py::build_argument_parser()` |
| `_create_engine()` | `engines/factory.py::create_engine()` |
| `_load_toc()` | `parsers/toc_loader.py::load_toc()` |
| `_get_output_path()` | `utils/paths.py::get_output_path()` |
| `_Tee` 클래스 | `utils/tee.py::Tee` |
| BOM 파이프라인 로직 (~150줄) | `pipelines/bom_pipeline.py` |
| 문서 파이프라인 로직 (~180줄) | `pipelines/document_pipeline.py` |

---

## Phase 9 전체 진행 현황

| Step | 내용 | 상태 |
|---|---|---|
| Step 1 | 유틸 모듈 7개 추출 | ✅ 완료 |
| Step 2 | `pipelines/` 패키지 | ✅ 완료 |
| Step 3 | `parsers/types.py` TypedDict | ✅ 완료 |
| **Step 4** | **main.py 슬림화** | ✅ **완료** |
| Step 5 | 아키텍처 테스트 | 미착수 |

---

## 다음 단계 (Step 5)

`tests/architecture/` 디렉토리 생성 후 레이어 의존성 위반 자동 검출 테스트 작성.
- `main.py` → `pipelines/` → `extractors/` → `engines/` 단방향 의존 규칙 강제
- `pytest-archon` 또는 직접 `ast` 파싱으로 구현 가능
