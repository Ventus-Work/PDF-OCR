# Phase 12 Step 12-2 결과보고서 — excel_exporter.py 분해

- **작성일**: 2026-04-22
- **실행자**: Claude Sonnet 4.6 (Thinking)
- **기준 커밋**: `826c4b4` (main)
- **롤백 태그**: `phase12-step2-start` (333de96 기준)
- **상위 기술서**: `05_검증_리뷰/Phase12_대형모듈_분해_상세_구현_기술서_20260422.md`

---

## 1. 목표 대비 실적

| 항목 | 목표 | 실적 | 비고 |
|---|---|---|---|
| `excel_exporter.py` 라인수 | ≤ 300 | **230줄** ✅ | |
| `excel_styles.py` 라인수 | ~120 | **61줄** ✅ | |
| `excel_classifier.py` 라인수 | ~180 | **147줄** ✅ | |
| `excel_builders.py` 라인수 | ~280 | **368줄** ⚠️ | 4개 빌더 집합, 단일 책임 유지 |
| pytest 수집 케이스 | ≥ 552 유지 | **546 passed + 6 skipped** ✅ | Step 12-1+12-2 shim 10건 추가 |
| 전체 회귀 실패 | 0건 | **0건** ✅ | |
| import 계약 변경 | 0건 | **0건** ✅ | |
| shim 테스트 추가 | +5건 | **+5건** ✅ | |

> ⚠️ `excel_builders.py` 368줄은 기술서 목표(~280) 초과. 단, 4개 빌더(`_build_estimate_sheet`, `_build_detail_sheet`, `_build_condition_sheet`, `_build_generic_sheet`)는 **단일 Excel 빌더 계층**이라는 단일 책임을 유지하며, 기술서 §2.1 "300줄 초과 모듈 ≤ 2" 허용 범위 내. Phase 13 이후 빌더별 파일 분리 여부 재검토.

---

## 2. 산출 파일 목록

| 파일 | 구분 | 라인수 | 내용 |
|---|---|---|---|
| `exporters/excel_styles.py` | **신규** | 61 | 스타일 상수(Fill/Font/Alignment/Border) + `_apply_style()` |
| `exporters/excel_classifier.py` | **신규** | 147 | `_classify_table`, `_is_number`, `_try_parse_number`, `_row_style` + 정규식 2개 |
| `exporters/excel_builders.py` | **신규** | 368 | 4개 시트 빌더 + `_DETAIL_HEADER_GROUPS` 상수 |
| `exporters/excel_exporter.py` | **수정** (shim화) | 230 | shim import + `_export_impl` + `export` 별칭 + `ExcelExporter` 클래스 |
| `tests/unit/exporters/test_excel_exporter_shim.py` | **신규** | — | import 무결성 5케이스 |

---

## 3. 함수 이동 실적

### 3.1 `exporters/excel_styles.py` 이동

| 심볼 | 원본 라인 | 이동 완료 |
|---|---|---|
| `_THIN`, `_THICK`, `_BORDER_ALL`, `_BORDER_HEADER` | L53~56 | ✅ |
| `_FILL_HEADER`, `_FILL_SECTION`, `_FILL_SUBTOTAL`, `_FILL_TITLE` | L58~61 | ✅ |
| `_FONT_HEADER`, `_FONT_TITLE`, `_FONT_SECTION`, `_FONT_SUBTOTAL`, `_FONT_BODY`, `_FONT_NOTE` | L63~68 | ✅ |
| `_ALIGN_CENTER`, `_ALIGN_LEFT`, `_ALIGN_RIGHT` | L70~72 | ✅ |
| `_apply_style(cell, ...)` | L204~212 | ✅ |

### 3.2 `exporters/excel_classifier.py` 이동

| 심볼 | 원본 라인 | 이동 완료 |
|---|---|---|
| `_classify_table(table)` | L79~110 | ✅ |
| `_RE_NUMBER`, `_is_number(val)` | L117~120 | ✅ |
| `_RE_NUMERIC`, `_try_parse_number(value)` | L126~160 | ✅ |
| `_SECTION_KEYWORDS`, `_SUBTOTAL_KEYWORDS` | L163~169 | ✅ |
| `_row_style(row, first_col_key)` | L171~197 | ✅ |

### 3.3 `exporters/excel_builders.py` 이동

| 심볼 | 원본 라인 | 이동 완료 |
|---|---|---|
| `_DETAIL_HEADER_GROUPS` | L302~312 | ✅ |
| `_build_estimate_sheet(ws, table, title)` | L219~293 | ✅ |
| `_build_detail_sheet(ws, table, title)` | L315~448 | ✅ |
| `_build_condition_sheet(ws, table)` | L455~489 | ✅ |
| `_build_generic_sheet(ws, table)` | L496~555 | ✅ |

### 3.4 `exporters/excel_exporter.py` 유지 (오케스트레이터)

| 심볼 | 역할 |
|---|---|
| `_export_impl(sections, output_path, *, title)` | 4개 하위 모듈 조합, Excel 저장 |
| `export = _export_impl` | 하위 호환 공개 API 별칭 |
| `class ExcelExporter(BaseExporter)` | BaseExporter 인터페이스 구현 |

---

## 4. Import Shim 설계 실적

```python
# exporters/excel_exporter.py (shim 핵심 구조)
from exporters.excel_styles import (
    _THIN, _THICK, _BORDER_ALL, _BORDER_HEADER,
    _FILL_HEADER, ..., _apply_style,
)
from exporters.excel_classifier import (
    _classify_table, _is_number, _try_parse_number, _row_style,
    _RE_NUMBER, _RE_NUMERIC, _SECTION_KEYWORDS, _SUBTOTAL_KEYWORDS,
)
from exporters.excel_builders import (
    _DETAIL_HEADER_GROUPS,
    _build_estimate_sheet, _build_detail_sheet,
    _build_condition_sheet, _build_generic_sheet,
)

__all__ = [전체 공개 심볼 선언]

def _export_impl(...): ...  # 오케스트레이터 유지
export = _export_impl       # 하위 호환 별칭
class ExcelExporter(...): ...  # 유지
```

---

## 5. 체크포인트 결과

| # | 검증 | 합격 기준 | 결과 |
|---|---|---|---|
| CP1 | `pytest tests/unit/exporters/` | 전수 통과 | **254 passed** ✅ |
| CP3 | `export, ExcelExporter, _classify_table, _try_parse_number` 등 import | 모든 심볼 유효 | **OK** ✅ |
| CP6 | 전체 suite (integration 제외) | 546 passed 유지 | **546p / 6skip** ✅ |
| CP-shim | `test_excel_exporter_shim.py` | 5 케이스 통과 | **5 passed** ✅ |
| CP7 | 파일별 라인수 | 각 ≤ 300 | `excel_exporter.py` 230 / `excel_styles.py` 61 / `excel_classifier.py` 147 / `excel_builders.py` 368⚠️ |

---

## 6. 리스크 평가 (실행 후)

| 리스크 | 발생 여부 | 비고 |
|---|---|---|
| 순환 import | ❌ 미발생 | styles ← classifier ← builders ← exporter 단방향 |
| shim 누락 심볼 | ❌ 미발생 | `__all__` 전수 선언 + shim 테스트 5건 방어 |
| Golden E2E 변화 | ❌ 검증 예정 | Step 12-3 완료 후 전체 통합 검증 |
| 테스트 회귀 | ❌ 미발생 | 546 passed, 0 failed |
| `excel_builders.py` 라인 초과 | ✅ 발생 | 368줄 (목표 ~280). 기술서 §2.1 허용 범위 내 |

---

## 7. Git 기록

| 태그/커밋 | 내용 |
|---|---|
| `phase12-step2-start` | 착수 직전 스냅샷 (333de96 기준) |
| `826c4b4` | Step 12-2 완료 커밋 |

---

## 8. 누적 진행 현황 (Phase 12 전체)

| Step | 대상 | 상태 | 커밋 |
|---|---|---|---|
| 12-1 | `table_parser.py` (613→187줄) | ✅ 완료 | `333de96` |
| 12-2 | `excel_exporter.py` (721→230줄) | ✅ 완료 | `826c4b4` |
| 12-3 | `bom_extractor.py` (591줄) | 🔲 미착수 | — |
| 12-4 | `text_cleaner.py` (394줄) | 🔲 선택 | — |
| 12-5 | `section_splitter.py` (380줄) | 🔲 선택 | — |

**500줄 초과 모듈**: 3 → 1건 (`bom_extractor.py`만 잔존)

---

## 9. 다음 단계

**Step 12-3**: `extractors/bom_extractor.py` (591줄) 분해
- 산출 모듈: `bom_sanitizer.py` + `bom_state_machine.py` + `bom_ocr_retry.py` + `bom_converter.py` + `bom_extractor.py` (shim)
- **최고 위험**: 상태머신 내부 클로저(`_flush_section`, `_is_bom_header` 등 nested 함수) — §7.4 클로저 분해 금지 원칙 엄수 필요
- 착수 전: `git tag phase12-step3-start` 부여
